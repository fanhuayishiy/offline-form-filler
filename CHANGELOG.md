# 更新日志(Changelog)

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## v0.3.1 · 2026-09-09

参数管理自由化 + 界面细节打磨。

**改进**

- 新建资料套只预置姓名/手机/邮箱三个基础参数(原来 18 个),其余按需添加
- 所有参数可删除(含基础参数;删除被映射绑定的参数会先确认;每套至少保留一个)
- 参数名可点击改名:自动同步识别词与映射绑定(所有资料套同名参数一起改)
- 「设置此字段」保存映射后立即重扫生效
- 弹窗按钮布局优化:底部数据按钮五等分单行,映射页清空按钮间距统一

## v0.3.0 · 2026-09-08

路线图新增两项落地。

**新增**

- 填充历史与撤销:每次填充记录改动前原值(保留最近 50 条),popup 新增「填充历史」标签页,一键把当前页面字段回滚到填充前(页面刷新后无法回滚)
- Excel .xlsx 直接导入:内置最小 xlsx 解析器(共享字符串/内联字符串/数字单元),与 CSV 共用建套逻辑;解压库 fflate 已 vendor 进仓库,仍零网络请求

**改进**

- 测试扩充至 25 项断言(填充历史、撤销回滚、xlsx 解析)
- Firefox 构建脚本同步 vendor 目录

## v0.2.0 · 2026-09-08

路线图六项全部落地。

**新增**

- 网站白名单:自动填充可限定只在名单内网站生效(选项页配置,子域名自动包含;手动填充不受限)
- 选项页(Options Page):白名单管理、数据备份(扩展详情页 → 「扩展选项」)
- CSV 批量导入资料套:首行为表头(列名=参数名),一行一套;支持引号转义
- 勾选框/单选组支持:参数值为布尔语义("是/否"、true/false)自动勾选;单选组按选项文本匹配
- 中英日三语别名字典(氏名/電話番号/メールアドレス/住所 等)
- Firefox 版:`npm run build:firefox` 生成 firefox/ 目录(事件页 + gecko id)

**改进**

- 测试扩充至 20 项断言(勾选框、单选组、CSV 解析)

## v0.1.1 · 2026-09-08

填充可靠性大修:重点解决"点填充提示注入失败"的一系列场景。

**修复**

- 补注入改为顶层 frame 优先,修复带跨域 iframe 页面(在线客服组件等)整体注入失败
- content.js 整体闭包隔离,重复注入(自动 + 手动)不再因顶层 const 重名而失败
- 找当前标签页改用 `chrome.tabs.query({active,currentWindow})`,不再依赖部分内核缺失的 `chrome.windows` API
- 扩展重载后,旧页面里驻留的旧脚本失联保护:提示一次「请刷新本页」并休眠,不再抛未捕获错误
- 所有注入失败路径透出具体错误信息,便于定位

**新增**

- 手动填充补注入后改用 DOM 事件触发,不再依赖消息端口,内核兼容性更好

## v0.1.0 · 2026-09-08

首个发布版本,纯离线网页表单自动填充扩展。

**新增**

- 字段自动识别:中英文别名字典,按 `name`/`id`/`placeholder`/`aria-label`/`label` 线索匹配
- 多套资料管理(新建/改名/删除/切换),填充实时使用当前套
- 自定义参数:参数名参与自动识别
- 手动映射:右键「设置此字段」,支持绑定资料参数(跟随资料)或固定值
- 自动填充开关:页面加载/SPA 动态表单自动扫描,已填写字段跳过
- 右键菜单:一键填充、设置此字段
- 数据导出/导入(JSON),跨浏览器/电脑迁移
- React/Vue 受控组件兼容(原生 setter + 事件派发)
- jsdom 断言测试 16 项

**技术**

- Manifest V3,零网络权限,无任何网络请求
- 纯 vanilla JS,扩展本体零依赖
